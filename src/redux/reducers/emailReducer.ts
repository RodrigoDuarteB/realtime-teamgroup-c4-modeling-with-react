const defaultValue = ''

const reducer = (state = defaultValue, { type, payload }: any) => {
    switch (type){
        case 'setEmail':
            
        default:
            return state
    }
}

export default reducer