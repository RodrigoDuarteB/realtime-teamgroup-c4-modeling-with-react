import Centered from './Centered'

const Loading = () => {
    return (
        <Centered classes="bg-primary-dark">
            <p className="text-secondary text-4xl font-bold">Cargando...</p>
        </Centered>
    )
}

export default Loading
